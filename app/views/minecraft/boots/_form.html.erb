<%= form_with(url: minecraft_boots_path, class: ('hidden' if server.world.nil?)) do |f| %>
  <%= f.hidden_field :server_id, value: server.id %>
  <%= f.submit 'Power On', class: 'dropdown-item' %>
<% end %>
