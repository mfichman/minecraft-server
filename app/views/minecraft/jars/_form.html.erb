<%= form_for([:minecraft, @server, @jar], html: { class: 'form' }) do |f| %>
  <div class="form-group">
    <%= f.label :version %>
    <%= f.text_field :version, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= f.label :url, 'URL' %>
    <%= f.text_field :url, class: 'form-control' %>
  </div>

  <%= f.submit class: 'btn btn-primary mt-4' %>
<% end %>
