<%= link_to(
      'Save',
      minecraft_saves_path(server_id: server.id), 
      class: "dropdown-item #{'hidden' if server.world.nil?}", 
      data: { controller: 'minecraft--saves', 'minecraft--saves-server-id': server.id, target: 'minecraft--saves.button' }, 
      method: :post, 
      remote: true) %>
