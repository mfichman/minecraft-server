<%= link_to(
      'Power Off',
      minecraft_shutdowns_path(server_id: server.id), 
      class: "dropdown-item #{'hidden' if server.world.nil?}", 
      data: {confirm: "Are you sure? Don't forget to save first!"},
      method: :post, 
      remote: true) %>
