<%= form_with(url: minecraft_shutdowns_path, class: ('hidden' if server.world.nil?)) do |f| %>
  <%= f.hidden_field :server_id, value: server.id %>
  <%= f.submit 'Power Off', class: 'dropdown-item' %>
<% end %>
