db:
  volumes:
  - ./volumes/db:/bundle/volumes/db

web:
  volumes:
  - ./volumes/db:/bundle/volumes/db

worker:
  volumes:
  - ./volumes/db:/bundle/volumes/db
  - ./volumes/minecraft:/minecraft/data
  - ./volumes/wireguard:/etc/wireguard

logger:
  volumes:
  - ./volumes/db:/bundle/volumes/db

minecraft:
  volumes:
  - ./volumes/minecraft:/minecraft/data

wireguard:
  volumes:
  - ./volumes/wireguard:/etc/wireguard

redis:
  ports:
  - "6379:6379"
