build:
  docker:
    web:
      dockerfile: Dockerfile.bundle
      target: web
    worker:
      dockerfile: Dockerfile.bundle
      target: worker

release:
  command:
  - bin/rails db:migrate

  #setup:
  #addons:
  #- plan: heroku-redis
  #- plan: heroku-postgresql

run:
  web:
    command:
    - bin/rails web
