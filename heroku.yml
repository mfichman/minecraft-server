build:
  docker:
    web:
      dockerfile: Dockerfile.bundle
      target: web
    worker:
      dockerfile: Dockerfile.bundle
      target: worker
    release:
      dockerfile: Dockerfile.bundle
      target: release

run:
  web:
    command:
    - bin/rails web
