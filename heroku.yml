build:
  docker:
    web:
      dockerfile: images/bundle/Dockerfile

release:
  image: web
  command:
  - bin/rails db:migrate

  #setup:
  #addons:
  #- plan: heroku-redis
  #- plan: heroku-postgresql

run:
  web: bin/rails web
