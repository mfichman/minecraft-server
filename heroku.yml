build:
  docker:
    web:
      dockerfile: Dockerfile.bundle
      target: web
    worker:
      dockerfile: Dockerfile.bundle
      target: worker

release:
  image: bundle
  command:
  - bin/rails db:migrate

  #setup:
  #addons:
  #- plan: heroku-redis
  #- plan: heroku-postgresql

run:
  web:
    image: bundle
    command:
    - bin/rails web
