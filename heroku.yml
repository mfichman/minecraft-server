build:
  docker:
    bundle: Dockerfile.bundle

release:
  image: bundle
  command:
  - bin/rails db:migrate

  #setup:
  #addons:
  #- plan: heroku-redis
  #- plan: heroku-postgresql

run:
  web: bin/rails web
